<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>หน้าแรก📷</title>
  <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #fbc2eb, #a6c1ee);
      font-family: 'Kanit', sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
 }
 
     @ keyframes slideUpFade{
       fromn {
         opacity: 0;
         transform: translateY (50 px);
       }
     {
       ton {
         opacity: 1;
 
       }
    }

    .card {
      background: rgba(255, 255, 255, 0.15);
      color: #fff;
      padding: 2.5rem;
      border-radius: 1.5rem;
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px); /* Safari support */
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
      width: 350px;
      text-align: center;
      animation: slideUpFade 1.3s ease-out forwards;
      opacity: 0;
      transform: translateY(50px);
   
    }

    .card h2 {
      font-size: 2rem;
      margin-bottom: 0.8rem;
    }

    .card h4 {
      font-weight: 400;
      margin-bottom: 1.8rem;
      color: #fefefe;
    }

    .card button {
      padding: 0.8rem 1.6rem;
      border: none;
      border-radius: 999px;
      background: linear-gradient(to right, #ff758c, #ff7eb3);
      color: white;
      font-weight: bold;
      font-size: 1rem;
      cursor: pointer;
      transition: background 0.3s ease, transform 0.2s ease;
    }

    .card button:hover {
      background: linear-gradient(to right, #f74d6c, #fa70a1);
      transform: scale(1.05);
    }
  </style>
</head>
<body>
  <div class="card">
    <h2>ยินดีต้อนรับ</h2>
    <h4>สามารถดาวน์โหลดรูปได้จากเว็บไซต์ BAS STUDIO</h4>
    <button onclick="window.location.href='login.html'">📥 ไปดูรูปและดาวน์โหลด</button>
  </div>
</body>
</html>
